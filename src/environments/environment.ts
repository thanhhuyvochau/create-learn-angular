export const environment = {
  production: false,
  apiBaseUrlHttp: 'http://localhost:8080',
  apiBaseUrlHttps: 'https://algocore.education/backend',
  // HTTP: resource server shares the same host as the backend API (no extra path suffix)
  resourceUrl: '',
};
